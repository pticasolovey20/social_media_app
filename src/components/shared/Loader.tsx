const Loader = () => {
	return (
		<div className="w-full flex justify-center items-center">
			<img src="/assets/icons/loader.svg" alt="loader" width={18} height={18} />
		</div>
	);
};

export default Loader;
